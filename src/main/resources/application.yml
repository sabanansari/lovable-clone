spring:
  application:
    name: lovable-clone
  datasource:
 #   url: jdbc:postgresql://localhost:9010/pgvector-test
    url: jdbc:postgresql://pgvector-service:9010/pgvector-test
    username: user
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format-sql: true

